/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // output: 'export',
  // distDir: 'build'
  experimental: {
    reactRefresh: false,
  },
}

module.exports = nextConfig
